#include "main.h"
/**
 * main - main block
 * _islower - Checks for lowercase letter
 * @c : character to check for lower case
 * Return (0) or (1)
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
