#include "main.h"
#include <stdio.h>

/**
 * main - main block
 * _islower - checks for lower case
 * Return (0) always success
 */
int _islower(int c)
{
	if (c <= '97'; c >= '122';)	
	{
		printf("c is lowercase");
		return (1);
	}

	else
	{
		printf("c is uppercase");
		return (0);
	}
}

