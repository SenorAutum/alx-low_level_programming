#include "main.h"

/**
 * main - main block
 * _islower - checks for lower case
 * @c : character to check for lower case
 * Return (0) or (1)
 */
int _islower(int c)
{
	return (c >= '97' && c <= '122');
}
