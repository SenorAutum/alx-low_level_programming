#include "main.h"
/**
 * mul - Multipllies two integers
 * @a : integer to be multiplied
 * @b : integer to be multiplied
 * Return: 0(success)
 */
int mul(int a, int b)
{
	int result;
	result = a * b;
	return (result);
}
