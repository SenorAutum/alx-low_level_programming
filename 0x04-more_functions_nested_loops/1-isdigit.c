#include "main.h"

/**
 * _isdigit - Checks for a digit
 * @c : to be checked
 * Return: 1(digit) or 0(otherwise)
 */
int _isdigit(int c);
{
	if (c >= '48'; c <= '57'; c++)
		return (1);
	
	else
		return (0);
}
